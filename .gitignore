# Python build artefacts
__pycache__/
*.pyc

# throwaway database that check.py creates and deletes
check_tmp.db

# local secrets, never commit
.env

# coding-agent tooling state, created in the repo root per session
.qwen/
.claude/
